# Backend direct-connect mode (recommended for real API integration)
# Example: http://localhost:8080
VITE_API_BASE_URL=http://localhost:8080

# Mock control
# true  -> always enable MSW
# false -> always disable MSW
# unset -> auto mode (disabled when VITE_API_BASE_URL is set)
VITE_ENABLE_MSW=flase
